--- Makefile.PL.orig	2016-05-12 18:39:06.000000000 +0300
+++ Makefile.PL	2016-05-12 18:41:12.000000000 +0300
@@ -8,22 +8,22 @@
 use 5.008;
 use ExtUtils::MakeMaker;
 
-use Devel::CheckLib;
+#use Devel::CheckLib;
 {
 local @ARGV;
 unshift(@ARGV, ExtUtils::MakeMaker::_shellwords($ENV{PERL_MM_OPT} || ''))
 if $ExtUtils::MakeMaker::VERSION >= 6.73_08;
-check_lib(
-header   => 'cmark.h',
-function => 'return CMARK_VERSION >= 0x001500 ? 0 : 1;',
-) or warn('libcmark 0.21.0 or higher not found'), exit;
+#check_lib(
+#header   => 'cmark.h',
+#function => 'return CMARK_VERSION >= 0x001500 ? 0 : 1;',
+#) or warn('libcmark 0.21.0 or higher not found'), exit;
 }
 
 my %WriteMakefileArgs = (
   "ABSTRACT" => "Interface to the CommonMark C library",
   "AUTHOR" => "Nick Wellnhofer <wellnhofer\@aevum.de>",
   "CONFIGURE_REQUIRES" => {
-    "Devel::CheckLib" => 0,
+#    "Devel::CheckLib" => 0,
     "ExtUtils::MakeMaker" => 0
   },
   "DISTNAME" => "CommonMark",

